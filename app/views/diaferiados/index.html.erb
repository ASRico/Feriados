<h1>Lista de dias feriados</h1>

<table>
  <tr>
    <th>Dectription</th>
    <th>Fecha</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @diaferiados.each do |diaferiado| %>
  <tr>
    <td><%= diaferiado.dectription %></td>
    <td><%= diaferiado.fecha %></td>
    <td><%= link_to 'Ver', diaferiado %></td>
    <td><%= link_to 'Editar', edit_diaferiado_path(diaferiado) %></td>
    <td><%= link_to 'Borrar', diaferiado, method: :delete, data: { confirm: 'Esta seguro?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Diaferiado', new_diaferiado_path %>
